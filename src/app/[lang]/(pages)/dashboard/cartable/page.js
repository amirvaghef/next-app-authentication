import styles from "./page.module.css";

export const metadata = {
  title: "Cartable",
};

const Cartable = () => {
  return <div className={styles.test}>Cartable</div>;
};

export default Cartable;
